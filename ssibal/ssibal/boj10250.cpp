#include<iostream>
using namespace std;
bool hotel[100][100] = { 0, };

int main(void) {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int Test_Case, w, h, n;
	int tmp1 = 0, tmp2 = 0;
	cout << 65 + ' ';
	cout << ' ';
}